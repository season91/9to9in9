using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// [IJumpable]
/// 점프하는 대상이 정해져 있으니
/// </summary>
public class IJumpable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
